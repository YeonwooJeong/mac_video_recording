# macOS 화면 녹화 앱 요구사항 정리

## 기본 앱

- macOS에서 동작하는 응용프로그램으로 제작한다.
- SwiftUI 기반의 macOS 네이티브 앱 형태로 제공한다.
- 실행 가능한 `.app` 번들을 생성한다.
- 더블클릭 실행용 `.command` 파일도 제공한다.
- 앱 아이콘을 생성해서 macOS 앱 번들에 포함한다.

## MP4 동영상 녹화

- 녹화 결과는 MP4로 저장한다.
- 비디오 기본 비트레이트는 `2200 kbps`로 설정한다.
- 비디오 비트레이트를 변경할 수 있는 입력창을 제공한다.
- 동영상 녹화 프레임 설정 메뉴를 제공한다.
- 동영상 프레임 기본값은 `30 fps`로 설정한다.
- 프레임 선택은 드롭다운 메뉴로 제공한다.
- 프레임 선택지는 `20 fps`, `30 fps`, `60 fps`를 제공한다.
- 프레임 드롭다운 영역은 텍스트가 잘리지 않도록 충분한 폭을 확보한다.

## MP4 오디오 녹음

- 동영상 녹화 시 오디오 녹음 기능을 제공한다.
- 오디오 녹음 ON/OFF 기능을 제공한다.
- 오디오 녹음이 OFF인 경우에는 오디오 스트림과 오디오 입력을 아예 생성하지 않는다.
- 오디오 기본 음질은 `64 kbps`로 설정한다.
- 오디오 음질을 변경할 수 있는 입력창을 제공한다.

## 녹화 대상 선택

- 전체 화면 녹화 기능을 제공한다.
- 특정 창 녹화 기능을 제공한다.
- 드래그해서 직접 지정한 영역을 녹화하는 기능을 제공한다.
- 창/화면 목록 새로고침 기능을 제공한다.
- 화면 녹화 권한이 없을 때 무한 권한 팝업이 뜨지 않도록 처리한다.
- 이미 권한을 허용했는데 목록이 비는 경우 앱 재실행 안내를 제공한다.

## 저장 경로

- 저장 폴더 경로 설정 메뉴를 제공한다.
- 저장 경로 기본값은 사용자 다운로드 폴더로 설정한다.
- 저장 폴더 열기 기능을 제공한다.
- 마지막 녹화 파일을 Finder에서 볼 수 있는 기능을 제공한다.

## GIF 생성

- GIF 생성 메뉴를 제공한다.
- GIF 기본 프레임은 `20 fps`로 설정한다.
- GIF 프레임을 변경할 수 있는 입력창을 제공한다.
- 특정 창 GIF 녹화 기능을 제공한다.
- 직접 영역 지정 GIF 녹화 기능을 제공한다.
- GIF 녹화 완료 후 편집 메뉴를 제공한다.
- GIF 편집 메뉴에서 GIF 사이즈를 변경할 수 있게 한다.
- GIF 편집 메뉴에서 화질을 조정할 수 있게 한다.
- 편집된 GIF는 원본과 별도의 새 파일로 저장한다.

## 실행 및 공유

- 앱 실행 명령을 안내한다.
- 공유용으로는 `.command` 파일이 아니라 `.app` 번들을 zip으로 묶어 제공한다.
- 현재 빌드는 Apple Silicon Mac용 `arm64` 앱이다.
- 다른 사용자가 실행할 경우 macOS 화면 기록 권한을 직접 허용해야 한다.
- 정식 배포를 위해서는 Developer ID 서명 및 notarization이 필요할 수 있다.

## GitHub 업로드

- 로컬 Git 저장소를 초기화하고 커밋을 생성한다.
- 원격 저장소는 `https://github.com/YeonwooJeong/mac_video_recording.git`로 설정한다.
- GitHub push에는 사용자 Personal Access Token 또는 SSH 인증이 필요하다.
- 원격 저장소에 기존 커밋이 있을 경우 `git pull` 후 다시 push해야 한다.
